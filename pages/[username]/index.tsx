import React, { ReactElement } from "react";

export interface UserProfilePageProps {}

const UserProfilePage = (props: UserProfilePageProps): ReactElement => {
	return (
		<main>
			<h1>User profile</h1>
		</main>
	);
};

export default UserProfilePage;
