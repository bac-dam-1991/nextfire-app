import React, { ReactElement } from "react";

export interface PostPageProps {}

const PostPage = (props: PostPageProps): ReactElement => {
	return (
		<main>
			<h1>Posts</h1>
		</main>
	);
};

export default PostPage;
