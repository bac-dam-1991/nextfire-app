import React, { ReactElement } from "react";

export interface AdminPostsPageProps {}

const AdminPostsPage = (props: AdminPostsPageProps): ReactElement => {
	return (
		<main>
			<h1>Admin</h1>
		</main>
	);
};

export default AdminPostsPage;
